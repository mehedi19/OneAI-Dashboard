/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allows the Arena live-preview origin during local development.
  allowedDevOrigins: ['3000-iys1cdhrpengakg8smi2m.e2b.app'],
};

export default nextConfig;
